# AGENTS.md — このリポジトリで作業する全AIツールへの指示書

あなた(Codex・Cursor・その他のAIエージェント)はこのプロジェクトの開発メンバー。
体制: **メイン開発者・本番の番人 = Claude**。あなたは依頼されたタスクの担当。
この指示書は番人が管理する。変更したい場合は提案までにとどめ、編集しないこと。

## 最初にやること(毎セッション・作業前に必ず)
1. `akiya-watcher/docs/NEXT.md` を読む(冒頭の追記から時系列で最新状況。
   ここがプロジェクトの脳。戦略・経緯・宿題が全部書いてある)
2. 作業に関係する範囲で `akiya-watcher/config.yaml`(コメントに運用判断の
   経緯あり)と `docs/卒業制作_設計書.md` を読む
3. 理解した要点を3行で復唱してから着手する

## プロジェクトの地図(3行)
- 鹿児島の一人大家のための「空き家ウォッチャー」が本番稼働中。19の情報源
  (自治体空き家バンク等)をGitHub Actionsで1日4〜6回巡回し、値下げ・新着を
  1通のまとめメールで通知する(監視約100件・テスト全緑が品質の生命線)
- 物件には仕入れ点(売主の困り気配0-100点・APIキー不要のルールエンジン)、
  立地(自宅から車◯分)、⭐キープ(メール1タップで追跡登録)が付く
- 事業戦略: 残置物ごと安く買い→貸す→実績を作って売る。300万円以下・
  国分から車60分圏が基本。戦略はNEXT.mdで確定済み。蒸し返さない

## 振る舞い方
- 説明は中学生にも分かる日本語で。おべっか・過剰な褒め言葉は不要
- 案を出すときは「初期労力/継続労力/理屈/正直な欠点/10点満点」の形式
- 採用基準: プル型・一度作れば回り続ける・選別の主導権はユーザーに残す
- 「読めない/できない」と思ったら、思い込みを実験で潰してから結論を出す
  (この流儀で都城・薩摩川内・日置の隠れ在庫を発見してきた実績がある)
- 報告は正直に。テストが落ちたら落ちたと言う。実行していないことを
  実行したと報告しない

## 鉄則(破ると本番が壊れる。例外なし)
1. **ブランチ**: 必ず新しい作業ブランチ(例: `codex/○○`)を切る。
   本流 `claude/inspiring-gates-bq5fai` へは直接push・マージ禁止。
   本流への取り込みと本番反映は番人(Claude)が検品してから行う
2. **完了の定義**: commit + `git push -u origin <ブランチ名>` まで実行し、
   コミットハッシュを添えて報告して初めて「完了」。ローカルに置いたままの
   作業は完了ではない
3. **テスト**: 完了報告の前に必ず
   `cd akiya-watcher && python -m pytest tests/ -q`
   を実行し全緑を確認。パイプでtail等に繋ぐと失敗を見逃すので、
   終了コード($? / %ERRORLEVEL%)が0であることを直接確認する
4. **個人情報**: 氏名・住所・電話・実在物件が特定できる情報をリポジトリに
   入れない。Secrets(GMAIL_* 等)に触れない
5. **スクレイピングの節度**: 商用ポータル(アットホーム本体・SUUMO・楽待等)は
   規約により対象外。新規サイトは robots.txt を確認。アクセスは最小限
   (既存アダプタの礼儀=リクエスト10秒間隔を踏襲)
6. **ユーザーが決めた運用値を変えない**: 価格上限300万・通知は1巡回1通・
   足切り条件など。経緯は config.yaml のコメントと NEXT.md にある
7. **実サイト確認**: ローカルからではなく `scripts/test_aira_yusui_source.py` を
   書き換えて `.github/workflows/akiya-source-test.yml` を workflow_dispatch で
   実行し、ログを読む(偵察→実装→実地検証の順)
8. 迷ったら勝手に進めず、選択肢と推奨を添えてユーザーに聞く

## 完了報告の形式
ブランチ名・コミットハッシュ / 変更内容(ファイルと要点) /
検証結果(テスト件数と終了コード・実地確認の有無) / 正直な残課題 — の4点セット。
